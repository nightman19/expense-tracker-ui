VITE_API_BASE_URL=http://127.0.0.1:8000

# Only needed once the backend has API_KEY set (i.e. once deployed).
# Leave unset for local dev — the backend skips auth entirely when its own
# API_KEY env var is unset, so nothing here is required until you deploy.
VITE_API_KEY=